features for dashboard

new machine can be added from dashboard if adding new machine through admin dashboard then we can also controll number of slots to be filled into that mongoDB

if we adding slots and machines also look for a way to automate this task like automatically 48 slots will be made avaialble inside the mongoDB

maybe no pieCharts in the admin Dashboards for now